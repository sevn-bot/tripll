# Bench bundle exemplar (G10)

This sealed reference bundle pairs prose with a minimal stylesheet for multi-artifact
quality-gauntlet comparison.

## Expected structure

- One README with operator-facing copy.
- One CSS file defining a single status badge class.

The build output must match both artifacts against this bundle.
