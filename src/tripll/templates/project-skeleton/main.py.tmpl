"""Primary module seam for {{project_name}}."""


def greet() -> str:
    """Return a greeting string.

    Returns:
        str: Greeting text.

    Examples:
        >>> greet()
        'hello from {{project_name}}'
    """
    return "hello from {{project_name}}"
