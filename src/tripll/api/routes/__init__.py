"""tripll.api.routes — JSON API route modules for the control plane."""

from __future__ import annotations

from tripll.api.routes import agents, config, events, runs, waves

__all__ = ["agents", "config", "events", "runs", "waves"]
