#!/usr/bin/env bash
# tripll git wrapper — blocks `git clean -x`/`-X`. Installed by W0 git guard.
# Prepends repo bin/ to PATH so plain `git` hits this script before Homebrew git.
set -euo pipefail

repo_root="$(cd "$(dirname "$0")/.." && pwd)"
real_git_file=""
if [[ -f "${repo_root}/.git" ]]; then
  gitdir="$(sed -n 's/^gitdir: //p' "${repo_root}/.git" | head -1 | tr -d '[:space:]')"
  if [[ -n "$gitdir" ]]; then
    real_git_file="${gitdir}/tripll-real-git-path"
  fi
fi
if [[ -z "$real_git_file" ]]; then
  real_git_file="${repo_root}/.git/tripll-real-git-path"
fi

if [[ -f "$real_git_file" ]]; then
  # shellcheck disable=SC1090
  real_git="$(<"$real_git_file")"
else
  real_git="$(PATH="${PATH//${repo_root}/bin:/}" command -v git)"
fi

if [[ ! -x "$real_git" ]]; then
  echo "tripll git wrapper: real git not executable at $real_git" >&2
  exit 1
fi

if [[ "${1:-}" == "clean" ]]; then
  shift
  exec "${repo_root}/scripts/git_clean_guard.sh" "$@"
fi

exec "$real_git" "$@"
