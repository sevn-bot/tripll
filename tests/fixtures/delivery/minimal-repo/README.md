# Delivery fixture repo

Minimal git target for Gap 1 manual E2E evidence: `tripll run <set> --integrate --deliver`
on a real (tiny) repository without touching the tripll checkout itself.

Used by `tests/test_delivery_live_fixture.py` and documented in
`docs/evidence/live-fixture-e2e-evidence.md`.
